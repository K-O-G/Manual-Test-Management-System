@model IEnumerable<Domain.Entities.CheckLists.CheckListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CheckListId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Procedure)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpectedResult)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheckListComment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastExecutionDateTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CheckListId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Procedure)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpectedResult)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheckListComment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastExecutionDateTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CheckListItemId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CheckListItemId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CheckListItemId })
        </td>
    </tr>
}

</table>
